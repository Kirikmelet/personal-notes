# 出来事の仕事

```tasks
sort by priority
group by priority
group by due date
group by scheduled date
hide recurrence rule
due tomorrow
hide urgency
is not recurring
```
## 繰り返するの仕事
```tasks
sort by priority
# group by recurrence
# recurrence includes ${recur}
group by recurrence
hide recurrence rule
not done
due today
hide urgency
#hide priority
#hide due date
#hide done date
#hide backlink
is recurring
```
# プロジェクト看板リスト
```dataviewjs
let index_kanban = dv.pages('#kanban').file
for (const i of index_kanban) {
	let tags = i.etags.filter(x => !x.contains("kanban")).toString()
	dv.el("p", `${dv.fileLink(i.name)}: ${tags}`)
	//dv.paragraph(i.etags.filter(x => !x.contains("kanban")).toString())
	//const tasks = i.tasks
	//	.filter(f => f.header.subpath !== "Archive")
	//	.filter(f => f.header.subpath !== "Done")
	//	.sort(f => !f.position)
	// dv.table(["仕事・記述", "リスト",],tasks.map(f => [
	// 	f.text, f.header.subpath
	//]))
}
```
# 学校話題
## 数学の副話題
```dataviewjs
let math_notes = dv.pages("#math/index").file
dv.list(math_notes.map(f => dv.fileLink(f.path)))
```
