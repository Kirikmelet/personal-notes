# 僕の用事です

```tasks
sort by priority
group by priority
group by due date
group by scheduled date
hide recurrence rule
due tomorrow
hide urgency
is not recurring
```
## 繰り返するの用事
```tasks
sort by priority
# group by recurrence
# recurrence includes ${recur}
group by recurrence
hide recurrence rule
due tomorrow
hide urgency
#hide priority
#hide due date
#hide done date
#hide backlink
is recurring
```
# 僕のプロジェクト
```dataviewjs
let index_kanban = dv.pages('"プロジェクト"').file
for (const i of index_kanban) {
	dv.el("h3", dv.fileLink(i.name))
	dv.paragraph(i.etags.toString())
	const tasks = i.tasks.sort(f => !f.position)
	dv.table(["仕事・記述", "リスト",],tasks.map(f => [
		f.text, f.header.subpath
]))
}
```
# 僕の学校話題
## 数学の話題
```dataviewjs
let math_notes = dv.pages("#math/index").file
dv.list(math_notes.map(f => dv.fileLink(f.path)))
```
